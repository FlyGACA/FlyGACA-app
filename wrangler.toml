# Cloudflare Pages project config for `wrangler pages deploy`. The Pages Function
# in functions/api/[[path]].ts proxies /api/* to the Firebase gateway.
# See docs/RUNBOOK-deploy.md.
name = "flygaca-app"
pages_build_output_dir = "dist"
compatibility_date = "2024-06-01"
