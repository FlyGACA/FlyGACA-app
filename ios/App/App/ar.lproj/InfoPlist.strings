/*
  InfoPlist.strings (Arabic — ar)
  Localized Info.plist values for the FlyGACA iOS shell.

  These keys override their Info.plist counterparts when the device language is
  Arabic. The name mirrors `common.appName` in src/i18n/ar.json so the
  home-screen label matches the product's brand everywhere.
*/

"CFBundleDisplayName" = "فلاي جاكا";
"CFBundleName" = "فلاي جاكا";
